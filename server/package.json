{
  "name": "fleet-api",
  "version": "1.0.0",
  "description": "API gratuite (REST + WebSocket temps réel) pour l'app : employés/droits, parc, entretien. Auto-hébergeable, persistance sur fichier, sans base de données payante.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
